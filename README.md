# grubdash-backend
This is the backend for the Thinkful's GrubDash project

After downloading the files, "npm install" in the terminal.
Once this has installed, enter "npm run dev" in ther terminal to start the backend API
